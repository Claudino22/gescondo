import { Module } from '@nestjs/common';

@Module({
  imports: [
    // Importar módulos aqui (ex: AuthModule, CondominiumModule, etc)
  ],
  controllers: [],
  providers: [],
})
export class AppModule {}